$(document).ready(function () {
    // Dark mode toggle için event listener
    $('#darkModeToggle').change(function () {
        $('body').toggleClass('dark-mode');
    });
});

jQuery.noConflict();
jQuery(document).ready(function ($) {
    // Burada jQuery'nin $ sembolünü kullanabilirsiniz
  /*  $('#category1Select').select2({ placeholder: "Kaydý seçin...", width: '100%' });*/
    // Diðer select elemanlarý için select2'yi etkinleþtirin
});
